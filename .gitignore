# Godot-specific
.godot/
# NOTE: *.import files are deliberately NOT ignored. Godot needs them to load
# assets; without them a fresh clone silently falls back to default resources.

# Build
bin/
obj/

# OS
.DS_Store
Thumbs.db

# IDE
.vs/
.vscode/
*.sln.DotSettings.user

# Generated by garage_layout.py — regenerates deterministically, don't version it
garage_*.png
